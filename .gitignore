# =========================================================================
# 文件名称: /.gitignore
# 功能描述: Git 版本控制系统的“视线防线”配置文件
# 目标受众: 产品经理、视觉/交互设计师、前端组件开发者。
# 
# 💡 概念普及：什么是 “.gitignore”？
# 它是代码仓库的“防卫围栏”。由于现代前端工程非常宠杂，有许多电脑自动下载的依赖、
# 本地编译产生的缓存、或者含有您私人密码的安全配置：
# 1. 如果把它们全传上 Git 库，不仅仓库会变得臃肿不堪（多达几十万个碎碎小文件），
# 2. 更致命的是会导致密钥泄露（如敏感的密码文件被恶意的蜘蛛爬虫抓走），
# 3. 因此，我们在这份文件里声明特定的规则。Git 会主动对这些路径“闭上双眼”，
#    永远忽视它们，绝不将它们跟踪和提交到代码托管网上！
# =========================================================================

# -------------------------------------------------------------------------
# 📦 海量依赖包隔离区
# -------------------------------------------------------------------------
# 说明: 我们本地通过 `npm install` 下载的所有第三方功能包（如 React, Tailwind 等）。
# 它们就像是零件零件库。任何人在拿您的代码运行时，只要执行 `npm install` 就会在本地
# 自动下载配好，无需在代码仓里占用云盘。必须将其忽略！
node_modules/

# -------------------------------------------------------------------------
# 🏗️ 生产环境编译产物输出区
# -------------------------------------------------------------------------
# 说明: 现代前端代码需要经过 Vite 等工具打包、摇树压缩、剔除无用样式，
# 最终在本地或者云端构建出的“高压缩只读 HTML/CSS/JS”包。
# 这些文件只应该存在于部署服务器，或者每次发布前临时生成，完全没有必要在版本库里频繁记录。
build/
dist/

# -------------------------------------------------------------------------
# 🧪 测试覆盖率遥测指标
# -------------------------------------------------------------------------
# 说明: 本地自动化测试运行后自动生成的代码逻辑覆盖率图表等结果，只读且随用随丢。
coverage/

# -------------------------------------------------------------------------
# 🍎 操作系统冗余临时元数据
# -------------------------------------------------------------------------
# 说明: macOS 苹果系统在每一个打开过的目录里悄悄生成的文件夹检索视图缓存。
.DS_Store

# -------------------------------------------------------------------------
# 📝 系统运行和调试日志
# -------------------------------------------------------------------------
# 说明: 系统跑起来时因异常中断而闪退产生的本地 trace 输出日志，不具备可阅读价值，忽略。
*.log

# -------------------------------------------------------------------------
# 🔒 私人敏感配置密钥盾
# -------------------------------------------------------------------------
# 说明: 所有真正带有您机密的局部 `.env` 配置文件（如 .env.local、.env.production 等）。
# 警告: 绝对不能上传！这是互联网上因疏忽导致泄密财产损失的头号杀手！
.env*

# -------------------------------------------------------------------------
# 👍 特例反选白名单
# -------------------------------------------------------------------------
# 说明: 叹号 “!” 代表反向排除。即便上面已经表示排除了所有 `.env*` 规则，
# 依然允许例外把 `.env.example`（那个安全无毒的空白模板）跟踪提交，引导其他小伙伴开发！
!.env.example
