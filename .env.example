# =========================================================================
# 文件名称: /.env.example
# 功能描述: 环境变量配置模版文件
# 目标受众: 产品经理、初学前端工程师、全栈开发者。
# 
# 💡 概念普及：什么是“环境变量” (.env)？
# 它好比是房子的“软装配方”。在不同的环境里（比如本地开发、线上测试、生产环境），
# 我们的电脑或服务器需要加载不同的参数（如不同的 API 密钥、数据库链接）。
# 为了确保安全，并且使代码与配置“解耦”，我们遵循 12-Factor 安全规范：
# 1. 绝对不要把真实的 API 密钥等敏感信息写在代码（如 JS/TS 文件）里提交！
# 2. 我们将真实的私密配方写在本地的临时隐藏文件 `.env` 中（此文件已被安全隔离，不提交到代码库）。
# 3. 而当前这个 `.env.example` 文件则是对外的“空配方图纸”，只列出配方的名字，起引导和声明作用。
# =========================================================================

# -------------------------------------------------------------------------
# 🔑 [AI 核心口令] GEMINI_API_KEY
# -------------------------------------------------------------------------
# 说明: 驱动 Google 强大的 Gemini 思考引擎所必备的钥匙（授权令牌）。
# 安全机制: 
#   1. Google AI Studio 会在云端运行时自动为您注入当前的真实密钥，保证全栈逻辑畅通无阻。
#   2. 如果您在外部调试，可通过 AI Studio 界面右上角的 "Secrets" 齿轮安全面板直接填写和托管。
# ⚠️ 注意: 绝对不能加 VITE_ 前缀！避免客户端前端代码不小心暴露此私密密钥。
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# -------------------------------------------------------------------------
# 🌐 [访问基准路由] APP_URL
# -------------------------------------------------------------------------
# 说明: 指向本应用在互联网上发布、部署的绝对基准根网址（例如：https://ais-pre-...）。
# 作用:
#   1. 用于后台生成自指型的调用链接。
#   2. 第三方第三方认证 (OAuth) 流程的回调地址拼接。
#   3. 前后端跨域访问 API 端点的校验。
# 自动逻辑: 云平台容器拉起时会自动解析底座，不需要您在云端手动修改。
APP_URL="MY_APP_URL"
